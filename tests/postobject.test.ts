/**
 * PostObject tests have been moved to tests/integration/postobject.test.ts
 * so they run with the same TDD harness as buckets/objects: Baseline (direct
 * MinIO), Proxy (Herald + MinIO), and Swift (Herald + Swift). Run:
 *   deno test tests/integration/postobject.test.ts --allow-env --allow-net --allow-sys
 */
